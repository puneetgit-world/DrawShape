@model DrawShape.Models.ViewModels.CanvasViewModel
@using Newtonsoft.Json;
@{

    var canvasSize = DrawShape.Helpers.ShapePathHelper.DetermineCanvasSize(Model.ShapeToDraw.Size);
}

<div>
    <canvas data-shape="@JsonConvert.SerializeObject(Model.ShapeToDraw)" id="shapeCanvas" width="@canvasSize.Width" height="@canvasSize.Height">
        Drawing shape
    </canvas>
</div>

@section scripts{
    <script src="~/Scripts/drawShape.js"></script>
    <script>
        drawShape.init("shapeCanvas");
    </script>
}

